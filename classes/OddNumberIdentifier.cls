public class OddNumberIdentifier {
    // Method to check if a number is odd
    public static Boolean isOdd(Integer num) {
        return Math.mod(num, 2) != 0;
        //This Change from VS Code
        //Change 2
    }
}